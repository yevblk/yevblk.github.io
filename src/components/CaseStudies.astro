<section id="case-studies" class="mb-12 reveal" role="region" aria-label="Case Studies section">
  <h2 class="text-2xl sm:text-3xl md:text-4xl font-heading font-bold mb-4 text-text underline decoration-accent/30 underline-offset-4 drop-shadow font-serif" aria-label="Case Studies">
    <span class="sr-only" aria-hidden="true">Case Studies</span>
    {"Case Studies".split("").map((char, i) => (
      <span style={`opacity:0;display:inline-block;transform:translateY(16px);transition:opacity 0.5s ${0.2 + i*0.04}s,transform 0.5s ${0.2 + i*0.04}s;`} class="headline-letter">{char === " " ? "\u00A0" : char}</span>
    ))}
  </h2>
  <p class="text-base sm:text-lg font-semibold drop-shadow-sm text-white/90 mb-6">Detailed analysis of my projects: from concept to implementation.</p>
  <div class="grid grid-cols-1 gap-6 sm:gap-8 font-mono w-full case-studies-grid">
    <slot />
  </div>
</section>

<style>
.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0,0,0,0);
  white-space: nowrap;
  border: 0;
  user-select: text;
  color: transparent;
}
</style> 