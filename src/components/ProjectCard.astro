---
export interface Props {
  title: string;
  description: string;
  source?: string;
  demo?: string;
  stack?: string[];
}
const { title, description, source, demo, stack = [] } = Astro.props;
---
<article class="bg-background rounded-xl p-6 transition transform hover:scale-[1.03] flex flex-col h-full text-left items-start justify-start font-sans">
  <h3 class="text-xl font-bold mb-2 text-text font-serif">{title}</h3>
  <p class="text-text-muted mb-4 flex-1">{description}</p>
  <div class="flex flex-wrap gap-2 mb-4 font-mono">
    {stack.map(tech => (
      <span class="flex items-center gap-1 bg-accent text-white px-2 py-0.5 rounded-full text-xs font-semibold shadow-sm">
        {tech === 'HTML' && <i class="fa-brands fa-html5"></i>}
        {tech === 'PWA' && <i class="fa-solid fa-mobile-screen-button"></i>}
        {tech === 'JavaScript' && <i class="fa-brands fa-js"></i>}
        {tech === 'CSS' && <i class="fa-brands fa-css3-alt"></i>}
        {tech === 'React' && <i class="fa-brands fa-react"></i>}
        {tech === 'Astro' && <i class="fa-solid fa-star"></i>}
        {tech === 'Tailwind CSS' && <i class="fa-solid fa-wind"></i>}
        <span>{tech}</span>
      </span>
    ))}
  </div>
  <div class="flex gap-4 mt-auto">
    {source && source !== "#" && source !== "" && (
      <a href={source} target="_blank" rel="noopener" class="text-accent font-semibold hover:underline transition-colors duration-200 flex items-center gap-1">
        <i class="fa-brands fa-github"></i> Source
      </a>
    )}
    {demo && demo !== "" && (
      <a href={demo} target="_blank" rel="noopener" class="text-accent font-semibold hover:underline transition-colors duration-200 flex items-center gap-1">
        <i class="fa-solid fa-arrow-up-right-from-square"></i> Demo
      </a>
    )}
  </div>
</article> 